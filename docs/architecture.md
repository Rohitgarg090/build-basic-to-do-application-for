```json
{
  "summary": "A minimal iOS-only to-do application built with Expo (React Native) using exclusively local device storage via AsyncStorage or SQLite. No backend, no database server, and no authentication required since this is a single-user personal app with all data persisted on-device. The stack selections for Django and Supabase are intentionally omitted as they introduce unnecessary complexity for a local-first personal tool.",
  "techDecisions": {
    "web": "None — iOS only, no web interface needed",
    "mobile": "Expo (React Native) with expo-sqlite or @react-native-async-storage for local persistence",
    "backend": "None — Django is overkill and unnecessary for a local-only personal app with no sync, users, or API",
    "database": "SQLite via expo-sqlite stored entirely on-device, no Supabase required since there is no cloud sync or multi-device support",
    "auth": "None — single personal user, no login needed, device ownership is implicit authentication",
    "hosting": "Apple App Store distribution only, or TestFlight for personal sideloading"
  },
  "apiDesign": "No API required. All data operations are local SQLite CRUD calls directly within the app. No REST or GraphQL layer exists.",
  "authStrategy": "No authentication strategy needed. The app is personal-use, single-user, and local-only. Device passcode/FaceID at the OS level provides implicit access control.",
  "markdownContent": "# Architecture: Simple iOS To-Do App\n\n## Overview\n\nA no-frills personal to-do application for iOS built with Expo. All data lives on the device using SQLite. No backend, no cloud database, no authentication, no network calls.\n\n---\n\n## ⚠️ Stack Corrections\n\nThe originally proposed stack included Django and Supabase. Both are **removed** from this architecture for the following reasons:\n\n| Proposed | Decision | Reason |\n|---|---|---|\n| Django | ❌ Removed | No backend logic, API, or server needed for a local app |\n| Supabase | ❌ Removed | No cloud storage, no sync, no multi-user — local SQLite replaces this |\n| Expo | ✅ Kept | Correct choice for iOS React Native development |\n\n---\n\n## Tech Stack\n\n```\n┌─────────────────────────────┐\n│        iOS Device           │\n│  ┌───────────────────────┐  │\n│  │   Expo (React Native) │  │\n│  │   - React Navigation  │  │\n│  │   - UI Components     │  │\n│  └──────────┬────────────┘  │\n│             │               │\n│  ┌──────────▼────────────┐  │\n│  │   expo-sqlite         │  │\n│  │   Local SQLite DB     │  │\n│  │   /todos table        │  │\n│  └───────────────────────┘  │\n└─────────────────────────────┘\n```\n\n### Mobile: Expo\n- **Framework**: Expo SDK (React Native)\n- **Navigation**: `expo-router` or `@react-navigation/native` (single stack, minimal screens)\n- **Storage**: `expo-sqlite` for structured local data\n- **Styling**: React Native StyleSheet or NativeWind (Tailwind for RN)\n- **Target**: iOS only (`ios` platform flag in app.json)\n\n### Database: expo-sqlite (On-Device)\n- Single SQLite file stored in the app's local document directory\n- No network connection ever required\n- Data persists across app sessions automatically\n\n---\n\n## Data Model\n\n### todos table\n\n```sql\nCREATE TABLE todos (\n  id        INTEGER PRIMARY KEY AUTOINCREMENT,\n  title     TEXT NOT NULL,\n  completed INTEGER DEFAULT 0,  -- 0 = false, 1 = true\n  created_at TEXT DEFAULT (datetime('now'))\n);\n```\n\nThat's the entire schema. No foreign keys, no joins, no migrations needed.\n\n---\n\n## App Screens\n\n```\n┌─────────────────┐\n│   Todo List     │  ← Main screen, shows all todos\n│   (Home)        │    tap to complete, swipe to delete\n└────────┬────────┘\n         │ tap '+'\n┌────────▼────────┐\n│   Add Todo      │  ← Single text input + save button\n│   (Modal)       │\n└─────────────────┘\n```\n\n### Screen 1: Todo List\n- FlatList of all todos\n- Tap item → toggle complete\n- Swipe left → delete\n- FAB or nav button → open Add screen\n\n### Screen 2: Add Todo (Modal)\n- Single TextInput for task title\n- Save button → insert into SQLite → dismiss modal\n- No due dates, no priorities, no tags\n\n---\n\n## Local Data Operations\n\n```javascript\n// All operations are synchronous-style SQLite calls\n\n// READ\nSELECT * FROM todos ORDER BY created_at DESC;\n\n// CREATE\nINSERT INTO todos (title) VALUES (?);\n\n// UPDATE (toggle complete)\nUPDATE todos SET completed = ? WHERE id = ?;\n\n// DELETE\nDELETE FROM todos WHERE id = ?;\n```\n\n---\n\n## Project Structure\n\n```\n/app\n  ├── index.tsx          # Todo list screen\n  ├── add.tsx            # Add todo modal\n  └── _layout.tsx        # Navigation layout\n/lib\n  └── db.ts              # SQLite init + all query functions\n/components\n  ├── TodoItem.tsx        # Single todo row component\n  └── EmptyState.tsx      # Empty list placeholder\napp.json                  # Expo config (ios only)\npackage.json\n```\n\n---\n\n## What Is Intentionally Excluded\n\n| Feature | Status | Reason |\n|---|---|---|\n| User authentication | ❌ | Single personal user, unnecessary |\n| Cloud sync | ❌ | Local-only by design |\n| Backend API | ❌ | No server needed |\n| Push notifications | ❌ | \"No extra features\" |\n| Due dates / reminders | ❌ | \"No extra features\" |\n| Categories / tags | ❌ | \"No extra features\" |\n| Android support | ❌ | iOS only |\n| Web support | ❌ | iOS only |\n\n---\n\n## Deployment\n\n- **Development**: `expo run:ios` or Expo Go\n- **Personal Use**: TestFlight (no App Store review needed for personal sideload)\n- **App Store**: Optional — requires Apple Developer account ($99/yr)\n\n---\n\n## Dependencies\n\n```json\n{\n  \"dependencies\": {\n    \"expo\": \"latest\",\n    \"expo-sqlite\": \"latest\",\n    \"expo-router\": \"latest\",\n    \"react-native\": \"latest\"\n  }\n}\n```\n\nFour dependencies. That's it.\n\n---\n\n## Architecture Decision Record\n\n**ADR-001: Remove Django**\nDjango requires a running server, network access, and deployment infrastructure. A personal local to-do app has no use case for any of these. Removed entirely.\n\n**ADR-002: Remove Supabase**\nSupabase provides cloud PostgreSQL, auth, and realtime features. None of these apply to a single-user local-only app. expo-sqlite provides everything needed with zero cost and zero complexity.\n\n**ADR-003: Use expo-sqlite over AsyncStorage**\n